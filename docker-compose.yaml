services:
    myservice:
       image: redis:7-alpine
       restart: always
    server:
       image: go-server:v2
       restart: always
       ports:
           - "8080:8080"
       environment:
              - REDIS_ADDR=myservice:6379
       depends_on:
              - myservice